# Copy to .env (create .env in frontend root). Never commit .env.
# For production build, set VITE_API_URL and VITE_GOOGLE_CLIENT_ID to your production values at build time.

# API base URL for contact form and auth (use production URL when building for deploy)
VITE_API_URL=http://localhost:5000

# Google OAuth 2.0 Client ID (Web) from Google Cloud Console — required for "Sign in with Google"
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
